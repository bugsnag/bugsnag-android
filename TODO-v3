- Re-add "Bugsnag" static access class (Bugsnag.register, etc)
- Finish/test MetaData merging and filtering
- Check strict mode
- Add missing tests
- Re-add jar building (for maven and adding to libs/)
- Update README


Build/test/release instructions
-------------------------------

Build a new .jar file:

    TODO

Build a new .aar file:

    gradle build

Build and release a new version to maven central:

    gradle uploadArchives

Build and install the example app:

    gradle example:installDebug
